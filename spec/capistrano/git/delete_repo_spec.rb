require 'spec_helper'

describe Capistrano::Git::DeleteRepo do
  it 'has a version number' do
    expect(Capistrano::Git::DeleteRepo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
